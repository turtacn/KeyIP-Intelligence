# =============================================================================
# KeyIP nginx API stubs — DISABLED for production delivery.
# =============================================================================
# This file is intentionally empty.
# All /api/ requests now proxy directly to the Go apiserver (keyip-apiserver:8080).
# See web/nginx.conf for the production reverse proxy configuration.
# =============================================================================
