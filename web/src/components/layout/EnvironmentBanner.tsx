import React from 'react';

/**
 * EnvironmentBanner has been deprecated.
 *
 * KeyIP-Intelligence is now a single-mode (LIVE) real software product.
 * There are no more mock/proxy mode switches.
 */
const EnvironmentBanner: React.FC = () => {
  return null;
};

export default EnvironmentBanner;
